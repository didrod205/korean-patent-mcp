# ROADMAP

## 원칙

도구는 3개다. 늘리기 전에 "이게 생사 판정과 직접 관련 있나"를 먼저 묻는다.

## 지금 상태

| 도구 | 상태 |
|---|---|
| `rights_alive` | 완료 |
| `verify_citations` | 완료 |
| `search_ip` | 완료 |
| 배포 (`npx korean-patent-mcp@latest`) | 패키징 완료, 퍼블리시 대기 |

## 다음에 할 수 있는 것 — 우선순위 순

### 1. 등록원부 실시간 조회 연동 (가장 가치 큼)

지금 가장 큰 한계는 **연차료 납부 여부를 확정 못 한다**는 것이다.
[등록원부 실시간 정보 조회 서비스](https://www.data.go.kr/data/15124946/openapi.do)가
등록번호 기준으로 **연차정보·존속기간·전용실시권**을 준다.

이쪽은 KIPRIS Plus를 거치지 않고 **공공데이터포털에서 직접 활용신청**이 되는 API다
(제공기관 지식재산처, XML+JSON). 지금 쓰는 patUtiModInfoSearchSevice는 KIPRIS Plus
가입·신청이 필요하고 유료 구간이 있으므로, 요금이 걸림돌이면 이쪽을 1차 소스로 두고
검색은 포기하는 구성도 검토할 만하다 — 다만 등록번호 기준이라 키워드 검색은 안 된다.

붙이면 사라지는 한계:
- `expiry_estimated: true` → 확정 만료일
- "연차료 미확인" 경고 → 실제 납부 이력
- `holder` 출원인 기준 → 실제 등록권자

**도구는 안 늘린다.** `rights_alive` 내부에서 등록번호가 있을 때만 한 번 더 호출한다.
호출량이 2배가 되므로 무료 등급(월 1,000회)에서는 옵트인이어야 한다.

### 2. 존속기간 연장등록 반영

의약품·농약은 최대 5년 연장된다. 지금은 추정 만료일이 실제보다 이르게 나온다.
연장등록 정보를 못 얻으면, 최소한 IPC가 A61K/A01N 계열일 때 경고를 강화한다.

### 3. `probe` 결과를 status.ts에 되먹이기

probe가 "판정 규칙이 모르는 상태값"을 뱉는다. 이걸 모아 `DEAD_PATTERNS`를 채운다.
실사용자 몇 명의 probe 출력이면 커버리지가 크게 오른다.

## 안 만드는 것

### `fto_screen` — 침해 가능성 스크리닝

유사도 판단 로직이 필요하다. 그리고 틀렸을 때의 손해가 다른 도구와 차원이 다르다.
"이 특허 살아있나"를 틀리면 다시 찾아보면 되지만, "침해 아니다"를 틀리면 소송이 온다.

만든다면 전제: 청구항 텍스트 확보 → 구성요소 분해 → 사람이 검토하는 워크시트 출력.
**판정을 내리지 않고 검토 재료만 준다.** 그 전까지는 안 만든다.

### 상표·디자인

권리 생멸 규칙이 다르다(상표는 10년 갱신 무한, 존속기간 계산이 특허와 완전히 다름).
같은 `status.ts`에 욱여넣으면 특허 판정까지 망가진다. 별도 서버가 맞다.

### 해외 특허

국가마다 상태 어휘와 존속기간이 다르다. 국내에서 정확한 게 먼저다.

### 도구 10개

도구가 늘면 LLM이 어느 걸 부를지 헷갈린다. 그 순간 이 서버의 유일한 약속이 흐려진다.
